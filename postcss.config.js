module.exports = {
    plugins: [
      require('tailwindcss')(),
      require('autoprefixer')(),
      // If you're using any custom PostCSS plugins, make sure they're added here as well.
    ],
  };
  